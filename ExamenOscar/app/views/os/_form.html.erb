<%= form_for(@o) do |f| %>
  <% if @o.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@o.errors.count, "error") %> prohibited this o from being saved:</h2>

      <ul>
      <% @o.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre %><br>
    <%= f.text_field :Nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
